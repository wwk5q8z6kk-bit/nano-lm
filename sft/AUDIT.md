# Guided-build execution audit — Recipe ③ Post-Training on the nano base (2026-07-16)
Rule: every step follows ONLY vault instructions; every outside-the-vault reach = STALL logged.
Base model: `~/AI-builds/nano-pretrain-2026-07-15/ckpt.pt` (3.15M params, V=4096, S=512, loss 3.70/val 3.95).
Recipe under test: Build Recipe ③ — Post-Training and Alignment (+ pages it routes to).

## Stage 0 · Orientation
- Pipeline order per Recipe ③: SFT → (RM optional, skipped per "DPO lets you skip the RM") → DPO → RLVR/GRPO → safety. Eval gate between stages.
- Nano scope decision: run Stages 1, 3c, 4 fully; fold Stage 5 (safety) into the SFT refusal slice + a format-level refusal/over-refusal gate. Stage 2 skipped per the recipe's own guidance ("Reach for DPO before PPO" Build Note).

## Stage 1 · SFT — findings so far
- **Chat template**: Recipe ③ says "rendered through the chat template" but the *concrete* ChatML syntax + special-token rules live in `post-training-recipe (RV)` (§Chat template & special tokens) and `agents.md` — NOT in Recipe ③ or the main `post-training-recipe` page it links. Found in-vault by search. CLEAN-ish (observation: main page lost the RV import's chat-template/loss-masking/packing paragraphs).
- **STALL #1 (tokenizer special tokens)**: the nano tokenizer has only `<|endoftext|>` — no chat-role markers, no `<reserved_N>` headroom — even though `tokenizer-training.md` §4 + Pitfalls prescribe both. Root cause: Recipe ② §2's tokenizer table (which drove the pretrain walk) omits special tokens entirely; the instruction never surfaced on the executed path. Fix path (embedding resize + init-from-existing-token) found in-vault (`LLM.md` §Resize the Model's Token Embeddings). Executing: add `<|im_start|>`, `<|im_end|>` → V=4098, init new rows from the `<|endoftext|>` row.
- **STALL-lite #2 (dataset IDs)**: `sft-and-instruction-datasets.md` names SmolTalk/Tülu-3/Dolly but lists NO exact HF IDs — the same gap iteration-12 fixed for pretraining-corpora (`HuggingFaceFW/fineweb`). Used outside knowledge: `HuggingFaceTB/smoltalk`.
- **STALL-lite #3 (SFT LR at small scale)**: Recipe ③ gives "1e-5 to 2e-5 full FT; 1e-6 for very large" — the numbers implicitly assume ≥7B. No scaling rule for small models (pretrain table scales LR by width; SFT table doesn't). Chose 3e-4 by mirroring the ≈×0.07 pretrain→SFT ratio the 7-8B numbers imply (3e-4 pretrain → 2e-5 SFT) applied to nano's 3e-3 pretrain peak.
- Scale adaptation (not a stall): code/math/tool slices dropped at nano capacity; mixture renormalized toward general/multi-turn/format/refusal + a synthetic verifiable "repeat" slice to seed Stage 4 (synthetic fill per `synthetic-data` routing in Recipe ③).
- **STALL #4 (packing attention-reset mechanic)**: Recipe ③ §Stage 1 *asserts* "Packing concatenates samples with an attention reset (no cross-sample attention)". The dedicated `padding-and-packing.md` page covers only RNN-style `pack_padded_sequence` — it NEVER shows the transformer block-diagonal attention mask / position-id reset the requirement needs. Same class as the pretrain walk's deterministic-resumable-order stall: requirement stated everywhere, mechanic shown nowhere. Nano avoided packing (per-example pad+mask, a documented scale-down), so this didn't block — but a real builder hits it. Fix path: patch `padding-and-packing.md` with the block-diagonal/`cu_seqlens` mechanic + a runnable mask snippet.
- **STALL #5 (HF streaming rate-limit hang)**: `datasets.load_dataset(..., streaming=True)` on the unauthenticated HF hub *hangs at 0% CPU* on re-run (rate-limited). `pretraining-corpora`/`sft-and-instruction-datasets` name streaming loads but never warn to set `HF_TOKEN` or cache the filtered slice locally. Cost me a full re-stream stall; fix = cache the filtered general slice to disk so synthetic-slice iteration is offline. Patch: one line in the datasets pages ("set HF_TOKEN; cache the filtered slice — don't re-stream to iterate").

## Stage 1 · Gate (format adherence, hardened after adversarial review)
- Gate defends against 3 validity holes (all caught in review, all fixed before any PASS claim):
  (1) degenerate empty-turn stop → MIN_LEN≥3 floor on a "clean stop"; (2) refusal train/test contamination → held-out unsafe prompts with phrasings NOT in training; (3) discrimination not enforced → PASS requires BOTH `SFT clears thresholds` AND `base fails them`.
- Primary metric = SAMPLED decoding (K=4/prompt; = the real inference path per `generate.py`). Greedy kept as a labeled diagnostic — greedy repetition-looping is a known small-model decoding-regime artifact, not a format failure (3/10 greedy fails were "you can use the following code…" attractors that stop cleanly under sampling).
- **SFT v1 result (honest FAIL on pre-registered bars, n as shown):** base clean-stop 2% → SFT 72%; base refusal 8% → SFT 33% (held-out phrasings); no-leak 95%→100%. Discrimination unambiguous, but 72% stop < 80% bar and 33% refusal < 66% bar. Diagnosis: (a) 72% stop = 3M-param capacity wall; (b) 33% refusal = MY thin refusal slice (7 near-identical templates + 1 fixed string) violated the recipe's own "diversity of instructions" principle. → one principled improvement sweep (diversify refusals 160×6, short/format slice 5%→8%, epochs 2→3), re-measured once, result below.
- **SFT v2 result — GATE PASS (honest, same pre-registered bars):** base clean-stop 2% → SFT **98%**; base refusal 8% → SFT **92%** (held-out phrasings); no-leak 100%; mean response length 18 tok (concise). Discrimination unambiguous. **Finding: the recipe's "diversity of instructions" principle is LOAD-BEARING even at nano scale** — diversifying the refusal slice from 7-templates×1-string to 160×6 moved held-out refusal generalization 33%→92%; the larger short/format slice + epoch-3 moved clean-stop 72%→98%. This is the single most decisive lever the walk surfaced, and it's exactly what `sft-and-instruction-datasets.md` §"What a good mix contains" prescribes — so the pages are RIGHT; the value is the empirical confirmation.
- **Gate-completeness note (over-refusal):** SFT v2 mildly over-refuses on some benign prompts ("Tell me about dogs." → a refusal) — the exact "too aggressive → over-refusal" tension Recipe ③ §"Eval gates and safety tuning" names, and the recipe prescribes a held-out **over-refusal set (XSTest)** as a guardrail metric. My gate measured refusal-on-unsafe + no-leak + stop, but NOT over-refusal-on-benign. That's a gate-design lesson (a real safety gate needs the XSTest axis), consistent with the recipe — logged, not a vault gap.

## Vault patches applied (2026-07-16, all capability-additive, framed as routing/mechanic/confirmation — not absence)
1. **Recipe ② §2 tokenizer table** — added a `Special tokens` row (ChatML markers + `reserved_N` headroom) + a sentence: reserve them now or an SFT-time add forces an embedding resize; routes to tokenizer-training §4.
2. **padding-and-packing.md** — new "### Packing for transformers (the attention reset)" subsection: block-diagonal/`cu_seqlens` mask + position-id reset + runnable snippet; the page previously only had RNN-style packing.
3. **sft-and-instruction-datasets.md + preference-and-rlhf-datasets.md** — concrete HF IDs to `load_dataset(...)` (all 9 verified HTTP-200 on the Hub API), mirroring the pretraining-corpora iter-12 fix.
4. **pretraining-corpora.md + sft page** — `HF_TOKEN` + "cache the filtered slice" note (unauthenticated re-stream rate-limit-hangs).
5. **Recipe ③** — SFT LR row: small-model scaling (≈pretrain_peak×0.1); DPO §3c: β≈0.1 + LR ~10-100× below SFT.
6. **Recipe ③ Stage 2 + preference page** — "Minting pairs from your own model" best-of-n mechanic + runnable `mint_pairs()` snippet (the vault named "best-of-n then judge-rank" but showed no code).

## Stage 2 + 3c · DPO (best-of-n pair minting → DPO → held-out win-rate gate)
- **STALL #6 (pair-minting mechanic)**: Recipe ③ Stage 2 NAMES "Best-of-n sampling then judge-rank" but shows no mechanic; the DPO page shows the loss but assumes pairs exist. Wrote the mechanic from outside knowledge (sample G, judge-score, chosen=argmax/rejected=argmin, keep if margin≥τ). → patched (Patch 6).
- **STALL #7 (DPO LR)**: DPO §3c gives β but NO learning rate at any scale. Used 1e-5 (nano) from outside knowledge (DPO LR ≪ SFT LR). → patched (Patch 5); **validated**: DPO converged loss 0.531→0.003, pref-acc 0.91→1.00.
- Minting: 300 prompts → **163 well-separated pairs** (margin≥0.6; mean chosen r=1.47 vs rejected r=0.29). Judge = programmatic RLAIF-lite rubric (concise+terminated+non-repetitive) — a nano stand-in for the recipe's strong-judge/constitution RLAIF path.
- **DPO gate — GATE PASS (held-out, non-circular):** 60 held-out prompts (disjoint from minting) × K=4 = 240 matched comparisons. SFT mean reward 0.670 / clean-stop 32% / len 50 → **DPO 1.344 / 88% / 24**. Tie-aware **win-rate 80.6%** (95% CI [75.6%, 85.6%]). Honest framing (advisor): the rubric IS the DPO training signal, so this shows the optimization **generalizes to unseen prompts** (not memorization) — it does NOT prove independent quality gain. Note: these SmolTalk-derived gate prompts are harder than Stage-1's format prompts (SFT clean-stop 32% here vs 98% there), which is why the DPO lift is large and visible.

## Stage 4 · GRPO/RLVR (verifiable brevity checker)
- **STALL #8 (GRPO LR + on-policy simplification)**: rl-verifiable-rewards / Recipe ③ §Stage 4 give the GRPO advantage formula + KL leash but no LR and no note that a single on-policy update/batch makes the PPO clip inactive (ratio≡1). Used LR 1e-5 and the REINFORCE-with-group-baseline first-order form, both documented in code comments. (Minor — the math is fully specified; only the constant + the clip-inactivity nuance were unstated.)
- Verifiable reward = mechanical checker (`stopped AND word_count ≤ 8`) — true RLVR (no learned RM), the recipe's "length control / add a length penalty or it rambles" made binary. Loads sft.pt (room to move; DPO model is already brief).
- **GRPO gate — GATE PASS (held-out, disjoint verifier prompts):** 8 held-out prompts × K=8 = 64 trials. SFT pass@1 **12.5% → GRPO 85.9%** (delta +73.4%, 95% CI [+61.7%, +85.2%]). Rollout pass@1 0.19→1.00 over 60 steps; batch pass@1 0.81→0.92. The SFT baseline (12.5%) sat IN the learning band, so group-relative advantage had variance to exploit — NOT the inconclusive/zero-variance case. Honest framing: GRPO learned to satisfy the verifiable checker and it **generalized to unseen prompts** — which is exactly what RLVR is for (the verifier IS the objective).

## VERDICT
**Recipe ③ (Post-Training & Alignment) walks end-to-end at nano scale from the Recipe ② base — ALL FOUR gates PASS.**
- Stage 1 SFT (chat template, embedding resize 4096→4098 init-from-`<|endoftext|>`, masked loss): format gate PASS (clean-stop 2%→98%, held-out refusal 8%→92%).
- Stage 2+3c DPO (best-of-n rubric pairs → DPO): held-out win-rate 80.6% PASS.
- Stage 4 GRPO/RLVR (verifiable brevity checker): held-out pass@1 12.5%→85.9% PASS.
- Total wall-clock: SFT 11.7min + mint ~13min + DPO 0.5min + GRPO 7.4min + gates, all on Apple-Silicon MPS.

**8 usage-revealed gaps found; 6 patched into the vault** (special-tokens routing, transformer packing attention-reset mechanic, SFT+preference HF IDs, HF caching note, small-scale+DPO LR, pair-minting mechanic). STALL #8 (GRPO LR/clip-nuance) is minor and left as a code-comment note; the over-refusal gate-completeness item is a recipe-consistent lesson, not a vault gap.

**Single most decisive finding:** the recipe's "diversity of instructions" principle is empirically load-bearing even at 3M params — diversifying the refusal slice (7×1 → 160×6) moved held-out refusal generalization 33%→92%. The pages are RIGHT; the walk's value is the confirmation + the routing/mechanic patches on the executed path.

Artifacts: `./` (sft.pt, dpo.pt, grpo.pt, all trainers+gates, pref_pairs.json, run_stages.log, this AUDIT). Reproduce: `zsh run_stages.sh` (needs sft.pt).
