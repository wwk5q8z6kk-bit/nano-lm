# NanoLM Core v2 — reset proposal (INACTIVE)

```text
STATUS = SUPERSEDED_PROPOSAL
ALSO = INACTIVE_ALTERNATIVE_STRATEGY
SUPERSEDED_BY = frontier/DEVELOPMENT_PLAN.md
ACTIVE_BRANCH = frontier/active-v1
ACTIVE_PRODUCT = Nano Runtime / Wedge v1 (papers/WEDGE_V1.md)
```

## What this was

A competing strategic reset that proposed:

- archive the scribe lineage as the center;
- stop Nano Runtime / Fabric / NanoScribe product ideas from driving the repository;
- make small-model training and generalization the **only** active track;
- build a 3M / 10M / 40M / 160M ladder;
- introduce a multi-domain `SupportShift` benchmark;
- pause product work until model evidence justifies it.

Duplicate upload copies of that proposal are the same document for authority purposes.

## Why it is not current authority

Development is underway on `frontier/active-v1` under `frontier/DEVELOPMENT_PLAN.md`.
Wedge v1 Phase 2/3 artifacts exist; classical-first product work is the implemented frontier.
Therefore this reset **must not** claim:

```text
NanoLM Core v2 = only active research track
```

## Preserved value

Keep this file as a **preserved alternative** and as a future small-model research plan.
Reactivation requires an explicit later owner decision that supersedes
`frontier/DEVELOPMENT_PLAN.md` — not silent README merging.

See `PROGRAM_AUTHORITY.md`.
